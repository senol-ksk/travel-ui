import { OperationResultType } from '@/app/reservation/types'

type IProps = {
  data: OperationResultType
}

export default function EmailTourOrderResult({ data }: IProps) {
  return <div>EmailTourOrderResult</div>
}
